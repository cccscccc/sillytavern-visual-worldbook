# 世界书卡面管理 —— 安装与使用说明

## 这东西解决什么问题

你导入的角色卡一多，世界书也跟着变多，而且世界书的名字有的跟卡一样、
有的很奇怪，隔很久回去玩就不知道哪本是哪张卡的了。

这个小插件做的事情很简单：**把世界书按「它属于哪张卡」铺开显示，
每本世界书上面直接贴着那张卡的图案。** 你看到卡面，就知道是哪本了。

## 它不会改你的东西

这点先说清楚，免得你担心：

- **不看内容、只读目录。** 插件只是去问酒馆"有哪些世界书、有哪些卡"，
  然后按卡上记录的绑定关系摆出来给你看。
- **不改任何文件名。** 世界书在磁盘上的名字、卡在磁盘上的名字，一个字都不动。
  面板里看到的名字是插件临时拼出来给你看的，关掉就没了。
- **不改卡的任何内容。** 不会往卡里写东西。
- **点一下只是打开查看。** 你点某个格子，插件帮你把酒馆自己的世界书编辑
  器打开——那个是酒馆自带的功能，你不动手它就什么也不会改。

出了一切问题，把插件文件夹删掉就恢复原样，不留任何痕迹。

---

## 装到哪里

**推荐方式：让酒馆自己装。**

酒馆里点「扩展」→「安装扩展」，把仓库地址填进去，点「只给我安装」：

```
https://github.com/cccscccc/sillytavern-visual-worldbook
```

装好后刷新一下酒馆页面就行。

**手动方式**：把整个 `worldbook-gallery` 文件夹，放到酒馆的

```
<SillyTavern>\public\scripts\extensions\third-party\
```

里面去。

**脚本方式**（Windows）：本文件夹里的 `install.ps1`。

```powershell
.\install.ps1 -Action install      # 安装
.\install.ps1 -Action uninstall    # 卸载
.\install.ps1 -Action status       # 看装了没
```

它会自己找酒馆在哪；找不到时手动指定：

```powershell
.\install.ps1 -Action install -TavernRoot "D:\SillyTavern"
```

---

## 怎么打开

装好之后：

1. 刷新一下酒馆页面（或者重启酒馆）。
2. 点酒馆右上角那个**插头形状的按钮**（扩展设置）。
3. 在里面找到 **「世界书卡面管理」**，点开它。
4. 点里面的 **「打开卡面面板」** 按钮。

右边会滑出来一个面板，你的世界书就按卡面铺开在那里了。

---

## 面板怎么看

每个格子里是一个世界书，从上到下：

| 位置 | 是什么 |
|---|---|
| 上面的图 | 绑定这本世界书的那张卡的图案 |
| 粗字 | 展示名。默认是「卡名 · 世界书名」，一眼能对上 |
| 小字 | 这本世界书有多少条，以及它的真实文件名 |

几个特殊情况，插件会明确标出来：

- **灰色的、虚线框** = 这本世界书没有任何卡在用，是孤立的。这种通常可以清理。
- **橙色边**，写着「卡里写了，但世界书文件不存在」= 有张卡指名要用某本世界书，
  但那本世界书现在没了。这种情况进游戏会出问题，需要处理。
- **图片位置是个文件图标** = 这本世界书没有卡绑定，所以没有卡面可显示。
- **右上角写着「N 张卡」** = 不止一张卡在用这本世界书。

面板上面还有：

- **搜索框** —— 打几个字，按卡名或世界书名过滤。
- **显示风格** —— 可以切成「只显示卡名」或「只显示世界书名」。
- **显示未绑定的世界书** —— 不想看那些孤立世界书就取消勾选。
- **重新扫描** —— 你刚导入了新东西，点一下刷新。

---

## 怎么删掉

不想要了，直接把 `worldbook-gallery` 这个文件夹删掉，刷新页面就行。
它没往任何别的地方写东西，删了就干净了。

---

## 一个前提要提醒你

这个插件「配得准不准」，取决于你的卡是怎么进来的：

- **卡自带世界书**（导入一张卡，世界书跟着一起来）→ **配得很准**，
  因为卡自己就记着"我用的是哪本世界书"。
- **卡和世界书分开单独导入** → 这种情况插件只能靠名字猜，
  有一部分会猜不出来，显示成「未绑定卡」。

你之前说的做法是整卡导入，那就属于第一种，效果最好。
