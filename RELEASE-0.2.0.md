# 世界书卡面管理 v0.2.0

> 把世界书按「它绑定哪张角色卡」铺开显示，每本世界书贴着那张卡的卡面。
> 这一版新增：**启用状态一眼可见 + 在面板里直接开关，和酒馆双向同步。**

---

## 这一版新增了什么

### 1. 世界书开没开，一眼就能看到

每张卡片左下角有一个状态标：

- **已开启** —— 卡片有一圈绿边、整体亮起
- **已关闭** —— 卡片整体压暗

导入的角色卡一多、世界书也跟着多的时候，
「到底哪几本在生效」以前得挨个去酒馆界面里点开看，现在扫一眼面板就知道。

### 2. 面板里直接开关，和酒馆双向同步

卡片下方多了一个开关按钮，点一下就能开／关这本世界书。

- **在面板里拨开关** → 酒馆那边的世界书状态跟着变，酒馆自己会弹一句「已开启：xxx」
- **在酒馆界面里勾选** → 面板里这张卡马上变成「已开启」，不用手动刷新

两边操作的是**同一份状态**，不会出现「面板说开着、酒馆说关着」的情况。

### 3. 顶部统计 + 「只看已开启」

- 统计行现在会写「**N 本已开启**」
- 工具栏新增「**只看已开启**」筛选，勾上之后网格里只剩正在生效的世界书

---

## 怎么用

装好之后（安装方式见下方），点酒馆右上角**插头图标（扩展设置）** → 找到 **「世界书卡面管理」** → 点 **「打开卡面面板」**。

右侧滑出面板：

| 位置 | 是什么 |
|---|---|
| 上面的图 | 绑定这本世界书的那张卡的卡面 |
| 左下角小标 | **已开启** / **已关闭** |
| 粗字 | 展示名，默认「卡名 · 世界书名」 |
| 小字 | 条目数 + 世界书的真实文件名 |
| 右上角「N 张卡」 | 多张卡共用这本世界书 |
| 最下面一行 | 开关按钮 + 状态文字 |

点卡片任意位置会打开酒馆自带的世界书编辑器（只读查看）；
点开关只切换启用状态，不会误触打开编辑器。

---

## 安装 / 更新

### 从 Git URL 安装（推荐）

酒馆里点 **扩展** → **安装扩展** → 填入：

```
https://github.com/cccscccc/sillytavern-visual-worldbook
```

点「**只给我安装**」，然后**刷新酒馆页面**。

### 更新到这一版

- 已经从 Git URL 装过的：在扩展列表里点一下**更新**（或直接刷新页面），
  就会拉到这版代码。
- 手动放的：把 `worldbook-gallery` 文件夹整个替换掉，刷新页面。

### 手动放置

把整个文件夹放到：

```
<SillyTavern>/public/scripts/extensions/third-party/worldbook-gallery
```

### Windows 脚本

```powershell
.\install.ps1 -Action install      # 安装
.\install.ps1 -Action update       # 更新（会先备份旧版）
.\install.ps1 -Action uninstall    # 卸载
.\install.ps1 -Action status       # 查看装没装
```

脚本会自动寻找酒馆目录；找不到时手动指定：

```powershell
.\install.ps1 -Action install -TavernRoot "D:\SillyTavern"
```

---

## 修掉的一个坑（用 Windows 脚本装的话请留意）

`install.ps1` 之前**没有 UTF-8 BOM**。Windows PowerShell 5.1 遇到这种文件会
按系统 ANSI 代码页（中文系统是 GBK）去解码，脚本里的中文注释就全变成乱码，
解析器在莫名其妙的位置报「缺少右括号」之类的错，脚本直接跑不起来。

这一版已经给文件加上了 BOM，并新增了一个自检 `_verify_ps1_encoding.mjs`
盯着这件事，防止以后再犯。

用酒馆的「从 Git URL 安装」的话不受影响 —— 那条路不走这个脚本。

---

## 关于「只读」

这个扩展的设计红线是**不碰你的东西**：

- 不写任何文件
- 不改世界书、角色卡在磁盘上的任何名字
- 不往角色卡里写任何数据
- 展示名是运行时临时拼出来的，关掉就没了

**唯一的例外是卡片上那个开关** —— 它改的是酒馆设置里「现在哪几本世界书开着」，
和你自己在酒馆界面里勾选、取消勾选是**同一件事**，不涉及任何文件读写。

删掉扩展文件夹即完全恢复原状，不留任何残留。

---

## 原理

对应关系的来源是**角色卡自身的绑定字段**：

```
character.data.extensions.world
```

每张角色卡的 PNG 里都记录了它绑定的是哪本世界书。读这个字段，
就能**准确地**（而不是靠名字猜）把卡面配给世界书。

另外会读取 `character.data.character_book`，识别出**卡片自带但尚未导入**的世界书。

---

## 配对准确度

| 导入方式 | 配对效果 |
|---|---|
| **整卡导入**（世界书随卡一起来） | **准确**。卡自身记着绑定关系，不靠猜。 |
| 卡与世界书分开单独导入 | 部分无法配对，会显示为「未绑定卡」。 |

推荐使用整卡导入。

---

## 自检（开发者）

仓库内附带不依赖浏览器的自检脚本：

```bash
node _selftest.mjs              # 配对逻辑，15 项
node _selftest_toggle.mjs       # 开关逻辑，22 项
node _verify_load.mjs           # 真加载模拟（验证能装能跑）
node _verify_ps1_encoding.mjs   # PowerShell 脚本编码，10 项
```

- `_selftest.mjs` —— 配对逻辑 15 项断言（正常绑定、重名导入产生的后缀、
  孤立世界书、缺失世界书、卡片自带世界书、三种展示名风格等）
- `_selftest_toggle.mjs` —— 开关逻辑 22 项断言（读状态、开、关、不重复操作、
  显式指定、名字含逗号的兜底、失败时不假装成功、空名字等）
- `_verify_load.mjs` —— 造一棵假的酒馆目录树真 `import()` 一次，
  确认扩展能加载、hook 存在、`init` 能跑起来
- `_verify_ps1_encoding.mjs` —— 确认 `install.ps1` 有 UTF-8 BOM、
  是合法 UTF-8、中文没坏、四个动作都在

全部通过：**15 + 22 + 10 项断言，加上真加载模拟，0 失败**。

---

## 已知限制

- 世界书名字里如果**含逗号**，面板的开关会绕开酒馆的斜杠命令通道，
  改用直接操作界面下拉框的方式。功能一致，万一不生效可以看浏览器控制台
  里的日志（前缀 `[Worldbook Gallery]`）。
- 开关只控制**全局启用**列表。酒馆还有「按角色卡绑定」「按聊天绑定」
  两套独立机制，本扩展不干预那两套。

---

## 兼容性

- 开发与验证基于 **SillyTavern 1.18.0**
- 依赖 `public/scripts/world-info.js` 与 `public/script.js` 的导出，
  这两个文件在酒馆更新时可能有变动

---

## 文件变更（0.1.0 → 0.2.0）

新增：
- `_selftest_toggle.mjs` —— 开关逻辑自检，22 项
- `_verify_ps1_encoding.mjs` —— PowerShell 脚本编码自检，10 项

修改：
- `index.js` —— 新增 `isWorldEnabled()` / `toggleWorld()` /
  `setWorldEnabledViaSelect()`；`buildCard()` 加入状态标与开关；
  新增 `onToggleClick()`、`refreshStatsOnly()`、自写 `toast()`；
  监听 `WORLDINFO_SETTINGS_UPDATED` 实现双向同步；工具栏加「只看已开启」
- `style.css` —— 开关、状态标、卡片启停视觉、右下角提示的样式
- `manifest.json` —— 版本 0.1.0 → 0.2.0
- `install.ps1` —— 新增 `update` 动作（带备份）；**补上 UTF-8 BOM**，
  修掉 PowerShell 5.1 下因中文乱码导致脚本跑不起来的问题
- `README.md` / `GUIDE-zh.md` —— 补充开关功能说明
- `_verify_load.mjs` —— 桩模块补上 `selected_world_info` / `onWorldInfoChange` 导出

---

## License

MIT

---

**Full Changelog**: 无（这是首个 Release，仓库之前没有打过 tag）
