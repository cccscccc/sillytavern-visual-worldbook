# v0.2.1 — 修好「开关拨过去了，酒馆原生面板却没变」

这是一个**修复版本**。如果你上一版遇到过「在插件里打开了世界书开关，插件里显示已开启，但酒馆自己的『已启用的世界书』面板还是没启用」—— 这个版本就是修这个的。

## 之前坏在哪

插件把数据改对了，但**没改到酒馆界面真正读的那份东西**。

酒馆里有两个名字一模一样的函数，都叫 `saveSettingsDebounced`：

| 在哪 | 干什么 |
|---|---|
| `world-info.js` 第 84 行（私有） | 把启用列表同步到 `world_info.globalSelect`，再存盘 |
| `script.js` 第 469 行（导出） | 只存盘，**不碰** `globalSelect` |

插件当时导入的是下面那个。于是：数据改了 ✅，`globalSelect` 没改 ❌。而酒馆的界面显示和存盘读的恰恰是 `globalSelect` —— 所以面板一直显示旧状态。

## 现在怎么修的

改完数据后，**借酒馆自己的回写通道**：

1. 把酒馆界面那个世界书下拉框的勾选状态对齐成我们要的样子
2. 触发它的 `change` 事件
3. 酒馆自己的处理函数接管：从下拉框读回选中项 → 覆盖启用列表 → 同步 `globalSelect` → 存盘 → 通知界面刷新

这条路是酒馆原生逻辑，所以天然不会不同步。

## 顺带加固的两处

**一、列表还是空的时候先热身**

酒馆那个下拉框不是天生有内容的，只有打开过世界书面板之后才会被填满。如果你从没打开过，插件会先自动刷新一次世界书列表（等价于帮你把面板打开一下），再执行开关。这一步只重画列表，不改内容、不写文件。

**二、下拉框里没有这本书时，绝不触发 change**

这点很关键。如果硬触发，酒馆会按一个空的下拉框把启用列表**整个覆盖掉**，等于把你的状态悄悄清空。所以插件只在确认这本书在下拉框里时才走回写通道。

## 验证

- **43 项**开关逻辑断言（上一版 22 项）。新增的关键一项是把 `globalSelect` 单独建模 —— 这正是「数据改了」和「酒馆界面变了」的区别所在。
- 做了**反向对照实验**：把旧实现放回去跑，测试精确复现了你看到的现象（`globalSelect` 停在旧值）。
- **11 个**酒馆符号逐个核对了真实性（含本次新增的 `updateWorldInfoList`），全部存在于 1.18.0 源码。
- 语法检查、真加载模拟、PowerShell 脚本编码检查：全部通过。

合计 **15 + 43 + 10 项断言 + 11 个符号**，全绿。

## 升级方式

**从 Git URL 装的**：在酒馆「扩展」页面对这个扩展点一下更新即可。

**手动放的**：把 `index.js`、`style.css`、`manifest.json` 覆盖过去，重启酒馆或重载页面。

> 注意：如果你的扩展装在 `public/scripts/extensions/third-party/`（**所有人可见**的位置），而你不是管理员，酒馆的删除按钮会返回 403 —— 这是酒馆的权限设计，不是插件的问题。重装时选「只给我安装」就不会有这个困扰。

## 完整改动

- `index.js` — 借酒馆 change 通道回写；新增 `ensureWorldListReady()` / `triggerWorldInfoChange()` / `paintToggle()`；`toggleWorld` 改为异步
- `_selftest_toggle.mjs` — 22 → 43 项
- `_verify_load.mjs` — 桩同步新增导入
- `_verify_api.mjs` — 增加 `updateWorldInfoList` 核对
- `README.md` — 自检说明、已知限制补充
- `manifest.json` — 版本号 0.2.1，`homePage` 指向本仓库
